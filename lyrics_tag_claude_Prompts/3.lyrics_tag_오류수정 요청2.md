현재 코드가 잘못되어 있다. 수정하라.

---

# 문제

Claude가 OpenAI API 방식으로 실행하려고 하고 있다.

예:

* OPENAI_API_KEY 요구
* openai 또는 OpenAI 사용

이 방식은 금지한다.

---

# 반드시 지켜야 할 조건

* API key 사용 금지
* openai 라이브러리 사용 금지
* 인터넷 호출 금지

---

# 올바른 방식

로컬 whisper 라이브러리를 사용하라:

import whisper

model = whisper.load_model("base")
result = model.transcribe(audio_file_path)

---

# 수정 지시

1. import openai 전부 제거
2. OpenAI 관련 코드 전부 제거
3. whisper 기반으로 전사 로직 다시 작성
4. /api/transcribe에서 정상 동작하도록 수정

---

# 결과 형식

[
{ text, start, end }
]

---

# 추가 요구

* CPU 환경에서 실행
* ffmpeg 사용 가능하도록 유지
* 기존 기능 유지

---

# 실행

코드 수정 후 서버 재실행하고
API key 없이 정상 동작하는지 확인하라.

문제가 있으면 계속 수정하라.

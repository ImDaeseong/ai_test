You are a senior cybersecurity engineer.
Create a defensive security scanning tool called "DefenseScan" with the following structure:

Project root: defensescan/
  main.py              ← CLI entry point
  requirements.txt     ← dependencies
  modules/
    __init__.py
    web_scanner.py     ← web vulnerability scanner
    system_scanner.py  ← windows system scanner
    reporter.py        ← JSON + console reporting

Requirements:
- Python 3.8+
- Windows 10 primary, Windows 11/Server compatible
- HTTPS support (requests + ssl module)
- Admin privilege detection and warning
- Non-destructive, passive detection only
- OWASP-aligned checks

Create all files with full implementation code.
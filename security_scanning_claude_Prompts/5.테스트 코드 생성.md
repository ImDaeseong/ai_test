Create test suite for DefenseScan:

1. test_web_scanner.py
   - Test against https://httpbin.org (safe public test target)
   - Test SSL certificate check
   - Test security header detection
   - Mock response tests for each check class

2. test_system_scanner.py
   - Test port scanner (localhost)
   - Test process list retrieval
   - Test registry access (read-only)
   - Test with/without admin rights

3. Create run_test.bat for Windows:
   - Check Python version
   - Install requirements
   - Run all tests
   - Display summary

Use pytest framework.
All tests must be non-destructive and safe to run on local Windows machine.